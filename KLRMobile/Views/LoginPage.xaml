<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="KLRMobile.Views.LoginPage"
             BackgroundColor="#4D4B50">
    <ContentPage.Content>
        <StackLayout Padding="10,0,10,0" VerticalOptions="Center">
            <Image Source="Logo" />
            <Label Text="Email Address" FontSize="Medium" TextColor="White"/>
            <Entry Text="{Binding Text, Mode=TwoWay}" FontSize="Medium" />
            <Label Text="Password" FontSize="Medium" TextColor="White" />
            <Entry Text="{Binding Text, Mode=TwoWay}" FontSize="Medium" IsPassword="true" />
            <Button VerticalOptions="Center" Text="Login" Clicked="LoginButton_OnClicked" BackgroundColor="Green"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>